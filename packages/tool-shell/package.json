{
  "name": "@stratusagent/tool-shell",
  "version": "0.7.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit --project tsconfig.test.json",
    "test": "node --test --experimental-strip-types"
  },
  "engines": {
    "node": ">=22.13 <23 || >=23.4"
  },
  "dependencies": {
    "@stratusagent/core": "workspace:*",
    "@stratusagent/executor-local": "workspace:*",
    "@stratusagent/plugins": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3",
    "@stratusagent/permissions": "workspace:*"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stratuslabs/agent.git",
    "directory": "packages/tool-shell"
  },
  "description": "Shell execution for Stratus agents: shell.run on the local-command executor, with a replaced environment, a pinned cwd, and bounded output",
  "keywords": [
    "stratus",
    "agent",
    "ai",
    "llm",
    "stratus-plugin",
    "tools",
    "shell"
  ],
  "publishConfig": {
    "access": "public"
  },
  "stratus": {
    "pluginVersion": 1,
    "contributes": {
      "tools": [
        {
          "name": "shell.run",
          "risk": "gated"
        }
      ]
    },
    "config": {
      "type": "object",
      "properties": {
        "cwd": {
          "type": "string"
        },
        "passEnv": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "env": {
          "type": "object"
        },
        "timeoutMs": {
          "type": "integer"
        },
        "maxOutputBytes": {
          "type": "integer"
        },
        "shell": {
          "type": "string"
        },
        "workspaceRoot": {
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  }
}
