{
  "name": "stratus-plugin-fixture-channel",
  "version": "0.0.0",
  "private": true,
  "description": "A plugin contributing a channel, so the channel seam and the host-owned transport-secret path can be tested through the real loader.",
  "type": "module",
  "exports": "./index.js",
  "stratus": {
    "pluginVersion": 1,
    "contributes": {
      "channels": [
        { "name": "fixture" }
      ]
    }
  }
}
